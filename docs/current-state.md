# 当前状态

## 项目

- 名称：`pi-init`
- 定位：用于 Pi 的项目初始化扩展，生成 `AGENTS.md`、项目记忆文档和项目级 Skill。

## 当前目标

- 保持默认脚手架简洁、中英文语义一致，并可通过 Pi 命令或工具安全生成。

## 已知状态

- 提供 `/init-project` 交互命令和 `init_project` 模型工具。
- 默认生成 `AGENTS.md`、四个项目记忆文档及 `.pi/skills/<slug>/SKILL.md`。
- 支持简体中文、英文、dry-run 和已有文件覆盖确认。
- 测试命令为 `npm test`。

## 待处理

- 暂无已知待处理事项。

## 最近一次更新

- 2026-08-04：补全项目事实并优化默认模板内容。
